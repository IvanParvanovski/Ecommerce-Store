import { HighlightLinkDirectiveDirective } from './highlight-link-directive.directive';

describe('HighlightLinkDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightLinkDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
